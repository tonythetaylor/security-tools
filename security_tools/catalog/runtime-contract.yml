service:
  name: test-sec-project
  role: python_web

  startup:
    type: http
    port: 5000

  readiness:
    exec_first: true
    http_paths:
      - /
      - /health

mode: standalone

policy:
  block_on_container_exit: true
  block_on_no_readiness: true